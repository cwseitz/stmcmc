from setuptools import setup, find_packages

setup = setup(
    name='stmcmc',
    description='Sequential tempered Markov Chain Monte Carlo in Python',
    version='1.0',
    packages=find_packages(),
)
